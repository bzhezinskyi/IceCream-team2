import SimpleLightbox from 'simplelightbox';
import 'simplelightbox/dist/simple-lightbox.min.css';
var lightbox = new SimpleLightbox('.gallery-ice a', {
  captionsData: 'alt',
  captionsDelay: 250,
});

// npm install simplelightbox
